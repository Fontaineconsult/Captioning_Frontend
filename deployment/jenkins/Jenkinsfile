#!groovy
def awsCredentials = [[$class: 'AmazonWebServicesCredentialsBinding',
                       credentialsId: 'DPRC_AWS_CREDS']]

pipeline {
    environment {
        AWS_REGION = 'us-west-2'
    }
    agent any
    triggers {
        githubPush()
    }
    stages {
        stage('Test') {

            steps {

                dir("backend") {
                    git url: "https://github.com/Fontaineconsult/accessiblebookchecker/"

                }


                sh 'ls'
                sh 'ls /backend'
            }

        }

    }

}

